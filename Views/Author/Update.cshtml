@model BookStoreApp.Models.Author;
@{
    
}
<div class="container" style="width:40%;">
    <h1>Update Author</h1>
    <form asp-action="Update">
        <input type="hidden" asp-for="Id">
        <div class="form-group">
        <label asp-for="AuthorName">AuthorName</label>
        <input asp-for="AuthorName"  class="form-control" />
        <span asp-validation-for="AuthorName" class="text-danger"></span>
    </div>
    <div>
        @if(@TempData["msg"]!=null){
            @TempData["msg"]
        }
    </div>
    <div class="mt-2">
        <button type="submit" class="btn btn-primary"><i class="bi bi-check"></i> Update</button>
        <a href="/Author/GetAll" class="btn btn-primary" ><i class="bi bi-arrow-return-left"></i></a>
    </div>
    </form>
</div>